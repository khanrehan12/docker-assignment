apiVersion: v1
kind: Service
metadata:
  name: docker-service
spec:
  type: NodePort
  selector:
    app: dockerassignment
  ports:
    - protocol: TCP
      port: 9696
      targetPort: 9696
      nodePort: 30007
